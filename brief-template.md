# Murder Mystery Brief

Fill this in and hand it to Claude. Anything left blank, Claude will ask about
or fill with a sensible default. Lines beginning with `#` or `(...)` are notes
to you and can be deleted.

## Setting
(Optional. e.g. "a snowed-in country manor, 1926" or "a tech startup's launch
party". If blank, Claude will choose one that fits the tone and cast.)


## Tone & voice
(Required. e.g. "1920s noir, wry and atmospheric"; "campy and over-the-top";
"cozy Agatha-Christie drawing room"; "gothic and ominous". This governs the
voice of every file.)


## Rounds
(Required. How many rounds of play. 3 is a good default. Round 1 introduces the
cast and the murder; the final round is the accusation and reveal.)


## Players
(Required. One per line: a name and a short description. The description seeds
the character — role, personality, relationship to others, anything you want
baked in. Add as many as you have guests.)

- Name —
- Name —
- Name —
- Name —
- Name —
- Name —

## The victim
(Required. Which character is killed. Must be one of the players above.)


## Victim mode
(Optional. One of:
  - "plays then dies"  — victim plays round 1, is killed before round 2, then
                         their player becomes a ghost/observer  [default, multi-round]
  - "dead at start"    — victim is already dead when the game begins
If blank, Claude uses the default for your round count.)


## The killer
(Required. Which character is guilty. Must be one of the players above, and not
the victim. Only the host and that one player will ever know.)


## Anything else
(Optional. Specific clues, relationships, plot beats, content to avoid, an
in-joke for the group, a real venue's rooms to use, etc.)
